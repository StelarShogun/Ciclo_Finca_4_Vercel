; Optional MySQL extensions for local CLI when the default php.ini omits them.
; Docker app image and GitHub Actions already enable pdo_sqlite for the test suite.
; Do not load mysqli here — the app container image does not ship that extension.
